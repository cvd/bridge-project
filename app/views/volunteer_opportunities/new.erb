<div class="volunteer-opp-form">
  <h1>Create New Volunteer Opportunity</h1>
  <% form_for @volunteer_opportunity, url(:volunteer_opportunities, :create), 
    :class => :form, :id => "volunteer-opp" do |f| %>

    <% if @volunteer_opportunity.service? %>
      <h3>For the service: <%= @volunteer_opportunity.service.site_name %></h3>
      <input type="hidden" name="volunteer_opportunity[service_id]" value="<%= @volunteer_opportunity.service_id %>" />
    <% end %>

	<%= f.hidden_field :status, :value => (@volunteer_opportunity.new_record? ? "pending": "updated" ) %>
	
    <fieldset>
      <ol>
        <li class="string">
          <%= f.label :title %><%= f.error_message_on :title %>
          <%= f.text_field :title %>
        </li>

        <li>
        <%= f.label :description %><%= f.error_message_on :description %>
          <%= f.text_area :description %>
        </li>

        <li class="string">
          <%= f.label :requirements %>
          <%= f.text_area :requirements %>
        </li>

        <li class="string">
          <%= f.label :population_served %>
          <%= f.text_field :population_served %>
        </li>

        <li class="string">
          <%= f.label :contact_name %>
          <%= f.text_field :contact_name %>
        </li>

        <li class="string">
          <%= f.label :contact_email %>
          <%= f.text_field :contact_email %>
        </li>
        
        <li class="string">
          <%= f.label :contact_phone %>
          <%= f.text_field :contact_phone %>
        </li>

        <li class="string">
          <%= f.label :hours_requested %>
          <%= f.text_field :hours_requested %>
        </li>

        <li class="string">
          <%= f.label :number_requested %>
          <%= f.text_field :number_requested %>
        </li>
      </ol>
    </fieldset>
    <fieldset>
      <%= f.submit "Save" %> |
      <%= link_to "Cancel", url(:volunteer_opportunities, :index) %>
    </fieldset>
  <% end %>
</div>
